

<h2 class="text-center mb-4">Bienvenue sur la plateforme de rendez-vous</h2>

<p class="text-center mb-5">
  Connectez-vous ou inscrivez-vous pour réserver un créneau avec un enseignant.
</p>

<% if (slots.length > 0) { %>
  <h4 class="mb-3">Créneaux disponibles</h4>
  <div class="table-responsive">
    <table class="table table-striped shadow-sm">
      <thead class="table-primary">
        <tr>
          <th>Date</th>
          <th>Heure</th>
          <th>Matière</th>
          <th>Réserver</th>
        </tr>
      </thead>
      <tbody>
        <% slots.forEach(slot => { %>
          <tr>
            <td><%= slot.date %></td>
            <td><%= slot.time %></td>
            <td><%= slot.subject || '---' %></td>
            <td>
              <% if (session && session.userId && session.role === 'etudiant') { %>
                <a href="/book/<%= slot.id %>" class="btn btn-success btn-sm">Réserver</a>
              <% } else { %>
                <span class="text-muted">Connexion requise</span>
              <% } %>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
<% } else { %>
  <p class="text-muted">Aucun créneau disponible pour le moment.</p>
<% } %>
